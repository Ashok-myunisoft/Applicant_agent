version: "3.9"

services:
  api:
    build: .
    container_name: applicant_scoring_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./temp:/app/temp
    restart: unless-stopped
